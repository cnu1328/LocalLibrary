extends layout 

block content 
    .books
        h1= title 

        ul 
            each genre in genre_list 
                li
                    a(href=genre.url) #[strong #{genre.name}]

            else 
                li No genres Found

extend layout 

block content 
    h1= title
    p Do you really wants to delete this Book Instance?
    form(method="POST")
        div.form-group
            input#genreid.form-control(type='hidden', name='genreid' value=genreid)

            button.btn.btn-primary(type='submit') Delete 
